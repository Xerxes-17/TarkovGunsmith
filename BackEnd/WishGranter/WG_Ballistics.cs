namespace WishGranter
{
    public class WG_Ballistics
    {
    //    // Changing the stats of the round based on the velocity on hit
    //    public void damageDrop_func(double impactSpeed, double initialSpeed)
    //    {
    //        double clampedRatio = Math.Clamp(impactSpeed / initialSpeed, 0, 1);
    //        double num5 = clampedRatio + (1d - clampedRatio) * Math.Clamp(damageDegradationCoefficient);

    //        double damage *= num5;
    //        double penetration *= num5;
    //    }

    //    public void parabola_func(double bulletMass, double bulletDiameterMM, double ballisticCoefficent)
    //    {
    //        var num1 = 0;

    //        var num2 = bulletMass / 1000d;
    //        var num4 = num2 * 2d;

    //        var num3 = bulletDiameterMM / 1000d;
    //        var num5 = num2 * 0.0014223d / (num3 * num3 * ballisticCoefficent);

    //        var num6 = num3 * num3 * 3.1415927d / 4d;

    //        var num7 = 1.2d * num6;

    //        var gravity = 10d;

    //    }

    //    public double calculated_drag_coef(double velocity, double someNumber)
    //    {
    //        int num = (int)Math.Floor(velocity / 343d / 0.05d); // Checks if the round is supersonic or not
    //        if (num <= 0)
    //        {
    //            return 0d;
    //        }
    //        if(num > someNumber)
    //        {
    //            return 0;
    //        }

    //        double num2 = 
    //    }
    }
}
